import { LabelBase } from "./LabelBase";

const { ccclass, property } = cc._decorator;

@ccclass
export default class LabelObject extends LabelBase {
    protected SetValue(value: string): void {
        //set Value cho 
    }

}
